wink.settings:
  type: config_object
  label: 'Wink Travel settings'
  mapping:
    client_id:
      type: string
      label: 'Wink Client ID'
    environment:
      type: string
      label: 'Environment'
    oauth2_client_id:
      type: string
      label: 'OAuth2 Client ID (server-side)'
    oauth2_client_secret:
      type: string
      label: 'OAuth2 Client Secret (server-side)'
